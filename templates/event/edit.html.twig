{% extends 'base.html.twig' %}

{% block title %}Edit Event{% endblock %}

{% block body %}
    <h1>Modifier une sortie</h1>

    {{ include('event/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <form method="post" action="{{ path('app_event_publish', {'id': event.id}) }}">
        <button type="submit" class="btn btn-primary">Mettre en ligne mon événement</button>
    </form>

{#    {{ include('event/_form.html.twig', {'button_label': 'Publier'}) }}#}

{#    MC: on ne sait pas si annuler veut dire annuler la sortie ou annuler les modif'#}
{#    {{ include('event/_form.html.twig', {'button_label': 'Annuler la sortie'}) }}#}

    {{ include('event/_delete_form.html.twig') }}

    <a href="{{ path('app_event_index') }}">Retour aux sorties</a>




    {#    {% if app.user and event.eventorgenazedby == app.user %}#}
{#        <a href="{{ path('app_event_show', {'id': event.id}) }}">• En savoir plus...</a>#}
{#        <a href="{{ path('app_event_edit', {'id': event.id}) }}">• Modifier</a>#}
{#    {% else %}#}
{#        <a href="{{ path('app_event_show', {'id': event.id}) }}">• En savoir plus</a>#}
{#        <a href="{{ path('app_subscription', {'eventId': event.id}) }}">• S'inscrire</a>#}
{#        #}{#  creer un if pour supprimer le bouton se desinscrire si on n'est pas inscrit #}
{#        <a href="{{ path('app_unsubscription', {'eventId': event.id}) }}">• Se désinscrire</a>#}
{#    {% endif %}#}



{% endblock %}
